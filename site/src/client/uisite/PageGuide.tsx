import React = require("react");

export const PageGuide: React.FunctionComponent = (props) => {
    return (
        <div
            style={{
                width: "75%",
                paddingTop: 100,
                display: "flex",
                justifyContent: "center",
            }}
            className={"main_text"}
        >
            Under Construction
        </div>
    );
};
