import React = require("react");
import {
    SpriteSheet,
    TextureCoordinate,
} from "../../services/resources/SpriteSheet";

interface SpriteComponentProps {
    textureCoordinate: TextureCoordinate;
}

export function getImgSpriteProps(props: SpriteComponentProps) {
    return {};
}
